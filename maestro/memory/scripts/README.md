# Memory Scripts

Status: placeholder for local tooling

Planned scripts:

- `memory-lint`: check absolute paths, invalid states, duplicate canonical owners, missing index entries, and closed runs in active paths.
- `memory-pack`: build a compact task-specific context pack from `memory-index.yaml` and `read-routes.yaml`.
- `archive-run`: move closed local runs to archive after durable deltas are extracted.

Do not add script behavior here until the memory layout stabilizes.

