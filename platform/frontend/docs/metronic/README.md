# Metronic Reference Pack Pointer

Status: reference pointer
Owner: frontend
Last audited: 2026-04-25
Canonical scope: compatibility pointer for relocated donor code

The raw Metronic donor pack no longer lives in active frontend docs.
Use `docs/ref/reference-code.md` and alias `reference-pack:metronic`.

This material is opt-in reference only.
It is not product truth and must not be read by default.

Allowed use:

- UI inspiration
- component extraction planning
- visual pattern review

Forbidden use:

- copying routing, auth, provider, or demo-page architecture as product code
- treating Metronic demo pages as product requirements
