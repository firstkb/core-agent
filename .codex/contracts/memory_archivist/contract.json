{
  "agent_id": "memory_archivist",
  "persona_name": "Archivist",
  "agent_version": "1.0.0",
  "contract_version": "1.0.0",
  "step": "memory",
  "role": "semantic docs and durable memory auditor",
  "artifact_language": "en",
  "input_schema": "input.schema.json",
  "output_schema": "output.schema.json",
  "shared_contracts": [
    "maestro/contracts/task-packet.schema.json",
    "maestro/contracts/stage-handoff.schema.json",
    "maestro/contracts/evidence.schema.json"
  ],
  "allowed_execution_modes": [
    "sub_agent",
    "inline"
  ],
  "default_invocation": {
    "mode": "compact_assignment",
    "description": "Use a self-contained compact assignment by default. Task-packet JSON is not required unless Maestro explicitly requests machine-readable memory audit continuity.",
    "expected_return": "Semantic drift findings, source-of-truth conflicts, recommended corrections, residual risks, and next action."
  },
  "machine_readable_invocation": {
    "mode": "task_packet_and_stage_handoff",
    "input_schema": "input.schema.json",
    "output_schema": "output.schema.json",
    "use_when": "Maestro explicitly requests machine-readable memory audit continuity, or memory/docs drift affects a gate."
  },
  "artifacts": {
    "handoff_json": {
      "path": "maestro/artifact/active/{work_slug}/handoff-memory-archivist-{attempt}.json",
      "media_type": "application/json",
      "role": "machine_readable_stage_handoff",
      "schema": "maestro/contracts/stage-handoff.schema.json",
      "authored_by": "ai",
      "required": false,
      "use_when": "Maestro explicitly requests machine-readable memory audit continuity, or memory/docs drift affects a gate."
    }
  },
  "recommended_next_agents": [
    "maestro_vnext"
  ]
}
