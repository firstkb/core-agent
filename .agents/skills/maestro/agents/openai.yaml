interface:
  display_name: "Maestro"
  short_description: "Owner-facing adaptive orchestration"
  brand_color: "#C2410C"
  default_prompt: "Use $maestro to route work, delegate only when useful, enforce gates, and reconcile evidence."

policy:
  allow_implicit_invocation: false
