# Design Tokens

Canonical token source for the frontend workspace.

Metronic is a donor for visual decisions, not the owner of this package. Tokens must be extracted here before any UI primitive migration starts in `ui-kit`.

## Boundary

- no React code;
- no business logic;
- owns color, spacing, radius, typography, and motion policy;
- consumed by `ui-kit` and apps.
