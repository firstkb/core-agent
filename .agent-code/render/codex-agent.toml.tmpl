# Generated by `node .agent-cli/bin/agent-stack.mjs render-runtimes`. Edit `.agent-code/` and rerender.
name = "{{system_name}}"
description = "{{agent_description}}"
nickname_candidates = ["{{skill_nickname}}"]
model_reasoning_effort = "{{codex_reasoning_effort}}"
developer_instructions = """
You are the Codex-native adapter for the system agent `{{system_name}}`.

Persona: {{persona_name}}
Skill nickname: {{skill_nickname}}

Before doing anything else, read and follow:
- AGENTS.md
- {{shared_agent_prompt}}

If this adapter conflicts with the shared source of truth, the shared prompt wins.

Critical invariants:
{{critical_invariants_plain}}
"""
