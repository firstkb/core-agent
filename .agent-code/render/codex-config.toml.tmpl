# Generated by `node .agent-cli/bin/agent-stack.mjs render-runtimes`. Edit `.agent-code/` and rerender.
# Keep user-specific model, approval, and sandbox defaults in ~/.codex/config.toml.
# This repo-scoped file only pins minimal project discovery behavior.
project_root_markers = [".git"]
