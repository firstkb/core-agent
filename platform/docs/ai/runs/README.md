# Retired Run Artifacts

Status: retired legacy run pointer

New Atlas run artifacts now live under:

- `ai-memory/runs/active/<task-id>/`

Closed Atlas run artifacts should live under:

- `ai-memory/runs/archive/<task-id>/`

The historical run folders in this legacy directory are provenance only.
Do not create new run folders here.
