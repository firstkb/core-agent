# Form Builder Storage And SQL View Contract

Status: compatibility pointer
Owner: frontend
Last audited: 2026-04-25
Canonical scope: old frontend-owned backend storage and SQL view path

This storage and SQL view contract has been compacted into:

- `platform/backend/docs/contracts/platform-studio-form-builder.md`

Use the backend contract for current runtime object naming, generated tables, canonical data views, grid views, multivalue tables, lookup-derived outputs, and additive runtime apply rules.
Older `vw_ps_*` examples in this path are superseded by the active `vw_*` and `vg_*` runtime naming direction.
Use git history only when auditing original storage/query detail.
