# Platform Admin Web

Internal platform and backoffice application.

## Boundary

- owns platform-level admin journeys;
- may compose shared packages from `../../packages`;
- must not be imported by any package.
