# EXTDB Legacy Reference Pack Pointer

Status: reference pointer
Owner: platform-studio
Last audited: 2026-04-25
Canonical scope: compatibility pointer for relocated legacy behavior source

The raw EXTDB legacy pack no longer lives in active Platform Studio docs.
Use `docs/ref/reference-code.md` and alias `reference-pack:extdb-legacy`.

This material is opt-in reference only.
It is not product truth and must not be read by default.

Allowed use:

- old field id archaeology
- legacy page settings and filter behavior review
- import, PDF, and report behavior comparison

Forbidden use:

- copying ASP, .NET, SQL, or old architecture into the current product
- treating legacy behavior as current contract without compaction into active docs
