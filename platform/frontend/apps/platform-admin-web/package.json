{
  "name": "@platform/platform-admin-web",
  "private": true,
  "version": "3.0.2",
  "type": "module",
  "dependencies": {
    "@platform/app-shell": "workspace:*",
    "@platform/auth-core": "workspace:*",
    "@platform/i18n": "workspace:*",
    "@platform/tenant-core": "workspace:*",
    "@platform/ui-kit": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.3.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:dev": "env PLATFORM_ADMIN_WEB_APP_ENV=DEV sh -c 'tsc --noEmit && vite build --mode development'",
    "build:prod": "env PLATFORM_ADMIN_WEB_APP_ENV=PROD sh -c 'tsc --noEmit && vite build --mode production'",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
