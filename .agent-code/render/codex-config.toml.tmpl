# Generated by `node .agent-cli/bin/agent-stack.mjs render-runtimes`. Edit `.agent-code/` and rerender.
#:schema https://developers.openai.com/codex/config-schema.json
# Keep user-specific approval, sandbox, and auth defaults in ~/.codex/config.toml.
# This repo-scoped file pins the project model and native agent role wiring.
project_root_markers = [".git"]
model = "gpt-5.4"

# Optional override used by /review.
# review_model = "gpt-5.4"

[features]
multi_agent = true

[agents]
max_depth = 1
max_threads = 4

{{codex_agents_registry}}
