# Standards Catalog

This folder is the canonical standards library for the Codex-native repo.

Use these files as reusable guidance for design, implementation, review, and orchestration work.

Reading order for repo/runtime work:

1. `runtime/repository.md`
2. `runtime/artifact-governance.md` for the active Maestro vNext artifact model
3. `shared/security.md`
4. relevant files under `engineering/`
5. relevant domain notes under `backend/`, `frontend/`, `infra/`, and `library/`

Current categories:

- `runtime/` for repository-specific runtime and artifact rules
- `shared/` for cross-cutting requirements
- `engineering/` for workflow and code quality expectations
- `backend/`, `frontend/`, `infra/`, `library/` for domain-specific design notes

Status:

- `.codex/standards/` is the active canonical set
